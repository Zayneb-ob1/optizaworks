# Source material

These HTML snapshots were supplied during the original content migration. They are not served by Next.js and are retained only as historical references for copy, organization names, and external links.

Runtime content lives in the SQLite database and the typed starter data under `shared/content/`.
